import * as CONSTANTS from '../../helpers/ConstantVars';
import classes from './ImageHolder.css';

const imageHolder = () => {

    return `<footer id="${ CONSTANTS.IMAGE_HOLDER }" class="${ classes.Container }"></footer>`
}

export default imageHolder;